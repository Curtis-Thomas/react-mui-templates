import { Divider, Typography } from "@mui/material";

function Home() {
  return (
    <>
      <Typography>MUI Templates</Typography>
      <Typography>A collection of React templates which use MUI.</Typography>

      <Divider />
      <Typography>Categories</Typography>
      <Typography>Dashboard</Typography>
      <Typography>Landing Page</Typography>
    </>
  );
}

export default Home;
